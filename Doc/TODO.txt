Rewrite fps meter using from collections import deque

Change room.getTilesaround so that number of tiles can be specified

make tiles stored in tilemaps instead of in individual files

make it so that invalid json files dont crash game

change event method so that events still work with low fps